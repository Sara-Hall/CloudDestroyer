/*
 * Main class
 * My notes: I never needed to change anything in this class.
 */
package BrickGame;

/**
 *
 * @author sara
 */
import javax.swing.JFrame;

// while I was coding the restart option for my game, I tried putting a loop around the main method.
// when I ran it, a ton of new windows came up, so I knew I didn't need to change this class.

public class Main {
    public static void main(String[] args) {
        JFrame game = new JFrame();
        Game Start = new Game();
        game.setBounds(10,10,600,700);
        game.setResizable(false);
        game.setVisible(true);
        game.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        game.add(Start);
    }
}
